import { Text, View } from "react-native";
import Header from "../Header";

export default function ConfigList(params) {
    return(
        <View>
            <Header title={"Choose Image to use"} />
            <Text>ConfigList</Text>
        </View>
        
    )
};
